# Dependencies
node_modules/

# Hardhat build artefacts (regenerable — must NOT be in the submission)
artifacts/
cache/
typechain-types/

# Secrets
.env
.env.*
!.env.example

# Runtime data generated by the demo
db/
oracle/inject.json

# Deployment records for local throwaway networks
deployments/localhost.json
deployments/hardhat.json

# Editor/OS noise
.vscode/
.idea/
Thumbs.db
